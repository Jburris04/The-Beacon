import test from 'node:test';import assert from 'node:assert/strict';import {execFileSync} from 'node:child_process';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
test('dry-run publisher makes no repository changes',()=>{const before=execFileSync('git',['status','--porcelain'],{cwd:root,encoding:'utf8'});const output=execFileSync(process.execPath,['scripts/publish-edition.mjs','examples/edition-003.example.json','--dry-run'],{cwd:root,encoding:'utf8'});const after=execFileSync('git',['status','--porcelain'],{cwd:root,encoding:'utf8'});assert.match(output,/Dry run complete/);assert.equal(after,before)});
